/** Страница статистики (заглушка — будет реализована в Этапе 10B). */
export default function StatsPage() {
  return (
    <div className="flex-1 flex items-center justify-center p-4">
      <p className="text-tg-hint text-lg">Статистика — скоро здесь</p>
    </div>
  );
}
