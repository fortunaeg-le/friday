/** Страница проектов (заглушка — будет реализована в Этапе 8). */
export default function ProjectsPage() {
  return (
    <div className="flex-1 flex items-center justify-center p-4">
      <p className="text-tg-hint text-lg">Проекты — скоро здесь</p>
    </div>
  );
}
